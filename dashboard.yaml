# Hive Local Dashboard
# Import this as a new dashboard in HA to get a dedicated sidebar entry.
#
# Settings → Dashboards → Add dashboard:
#   Title: Hive
#   Icon:  mdi:radiator
#   URL:   hive
#   ✓ Show in sidebar
#   ✓ Require admin (optional)

title: Hive
icon: mdi:radiator
views:
  - title: Hive Heating
    path: hive
    icon: mdi:radiator
    type: sections
    sections:
      - type: grid
        columns: 1
        cards:
          - type: custom:hive-local-dashboard-card
